name: Probe minimal dispatch plus inputs and release
on:
  workflow_dispatch:
    inputs:
      tag:
        type: string
        required: false
      deploy-docs:
        type: boolean
        required: false
        default: true
  release:
    types: [published]
jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - run: echo ok
