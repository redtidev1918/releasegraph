name: Probe probe-p1
on:
  workflow_dispatch:
    inputs:
      tag:
        type: string
        required: false
      deploy-docs:
        type: boolean
        required: false
        default: true
  release:
    types: [published]
# docsite-managed-file: probe
# docsite-managed-version: 2
# 刷新下载页（docs/download.md 与语言镜像）。
# 触发方式（按优先级）：1. workflow_dispatch 2. release: published 兜底。

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - run: echo ok
